// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "com.jerotoma"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
